'EJERCICIO 5.2

'estaturas.dat
'Fuente: estudiantes universitarios, elaboración propia
'Descripción :

'col 1 : estatura del estudiante en cm
'col 2 : peso del estudiante en gramos 
'col 3 : sexo del estudiantes 1 mujer, 0 hombre
'col 4 : altura de la madre en cm 
'col 5 : altura del padre en cm 

'A: CARGA DE DATOS

DATOS <- ("C:/DATOS/DOCUMENTACION/DOCTORADO/MODELOS/R/LU00025101/estaturas.dat",header=F,sep="\t", quote="")

ESTATURA <- as.numeric(DATOS$V1)
PESO <- as.numeric(DATOS$V2)

'B: CONSTRUCCION DEL MODELO

MODELO <- PESO ~ ESTATURA

'C: ESTIMACION DE PARAMETROS

LM <- lm(MODELO)
summary(LM)

'D: INFERENCIA

RESIDUOS <- LM$residuals
RESIDUOS <- sort(RESIDUOS)
RESIDUOS

par(mfrow = c(2,1))
hist(RESIDUOS)
boxplot(RESIDUOS,horizontal = TRUE)

outlierDetector <- function(DATOS){
	for
}

library(outliers)
OUTLIER <- outlier(RESIDUOS)

